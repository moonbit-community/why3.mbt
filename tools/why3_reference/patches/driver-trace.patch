diff --git a/src/core/trans.mli b/src/core/trans.mli
index ece519dc2..c5f006ed7 100644
--- a/src/core/trans.mli
+++ b/src/core/trans.mli
@@ -33,6 +33,14 @@ val bind_comp : ('a * task) trans -> ('a -> 'b trans) -> 'b trans
 
 val trace_goal : string -> task trans -> task trans
 
+(** Install a process-local, read-only task checkpoint callback while the
+    supplied function runs. The previous callback is restored on return or
+    exception. This hook is intended for pinned reference builds only. *)
+val with_checkpoint_callback :
+  (string -> task -> unit) -> (unit -> 'a) -> 'a
+
+val checkpoint : string -> task trans
+
 (** {2 Compose transformations} *)
 
 val compose   : task trans -> 'a trans -> 'a trans
diff --git a/src/core/trans.ml b/src/core/trans.ml
index 4375a0f67..09eaa0446 100644
--- a/src/core/trans.ml
+++ b/src/core/trans.ml
@@ -56,6 +56,23 @@ let trace_goal msg tr =
     end;
     new_task
 
+let checkpoint_callback = ref (fun (_ : string) (_ : task) -> ())
+
+let with_checkpoint_callback callback action =
+  let previous = !checkpoint_callback in
+  checkpoint_callback := callback;
+  try
+    let result = action () in
+    checkpoint_callback := previous;
+    result
+  with exception_ ->
+    checkpoint_callback := previous;
+    raise exception_
+
+let checkpoint name task =
+  (!checkpoint_callback) name task;
+  task
+
 let fold fn v =
   let h = Wtask.create 63 in
   let rewind acc task =
diff --git a/src/driver/driver.mli b/src/driver/driver.mli
index e5bc3ac91..bcf1d2a12 100644
--- a/src/driver/driver.mli
+++ b/src/driver/driver.mli
@@ -15,6 +15,22 @@
 
 type driver
 
+(** Read-only projection used by the pinned differential reference. It exposes
+    the fully resolved driver update data in the exact map/set fold order. *)
+type reference_profile_view = {
+  reference_printer : string option;
+  reference_filename : string option;
+  reference_transforms : string list;
+  reference_preludes : string list;
+  reference_theory_preludes : (Ident.ident * string list) list;
+  reference_theory_uses : (Theory.theory * Theory.theory) list;
+  reference_metas : (Theory.theory * Theory.tdecl list) list;
+  reference_blacklist : string list;
+  reference_result_parser : Call_provers.prover_result_parser;
+}
+
+val reference_profile_view : driver -> reference_profile_view
+
 (** Loading drivers *)
 
 val load_driver_file_and_extras :
@@ -86,6 +86,12 @@ val prove_task :
 (** Apply driver's transformations to the task *)
 val prepare_task : driver -> Task.task -> Task.task
 
+(** As [prepare_task], while invoking the read-only callback after driver
+    update, after instrumented composite-transform substeps, and after every
+    top-level driver transformation. Intended for pinned reference builds. *)
+val prepare_task_trace :
+  (string -> Task.task -> unit) -> driver -> Task.task -> Task.task
+
 val print_task_prepared :
   ?old       : in_channel ->
   driver -> Format.formatter -> Task.task -> Printer.printing_info
diff --git a/src/driver/driver.ml b/src/driver/driver.ml
index 8ab38fac6..0206c4d3b 100644
--- a/src/driver/driver.ml
+++ b/src/driver/driver.ml
@@ -49,6 +49,50 @@ type driver = {
   drv_tag         : int;
 }
 
+type reference_profile_view = {
+  reference_printer : string option;
+  reference_filename : string option;
+  reference_transforms : string list;
+  reference_preludes : string list;
+  reference_theory_preludes : (Ident.ident * string list) list;
+  reference_theory_uses : (Theory.theory * Theory.theory) list;
+  reference_metas : (Theory.theory * Theory.tdecl list) list;
+  reference_blacklist : string list;
+  reference_result_parser : Call_provers.prover_result_parser;
+}
+
+let reference_profile_view drv =
+  let theory_preludes =
+    Mid.fold (fun key values result -> (key, values) :: result)
+      drv.drv_thprelude []
+    |> List.rev
+  in
+  let theory_uses =
+    Mid.fold (fun _ value result -> value :: result) drv.drv_thuse []
+    |> List.rev
+  in
+  let metas =
+    Mid.fold
+      (fun _ (theory, declarations) result ->
+        let declarations =
+          Stdecl.fold (fun declaration values -> declaration :: values)
+            declarations []
+          |> List.rev
+        in
+        (theory, declarations) :: result)
+      drv.drv_meta []
+    |> List.rev
+  in
+  { reference_printer = drv.drv_printer;
+    reference_filename = drv.drv_filename;
+    reference_transforms = drv.drv_transform;
+    reference_preludes = drv.drv_prelude;
+    reference_theory_preludes = theory_preludes;
+    reference_theory_uses = theory_uses;
+    reference_metas = metas;
+    reference_blacklist = drv.drv_blacklist;
+    reference_result_parser = drv.drv_res_parser }
+
 (** parse a driver file *)
 
 let load_plugin dir (byte,nat) =
@@ -360,7 +360,7 @@ let update_task = let ht = Hint.create 5 in fun drv ->
     | task -> update task
 
 (* Apply driver's transformations to the task *)
-let prepare_task drv task =
+let prepare_task_trace trace drv task =
   let lookup_transform t = lookup_transform t drv.drv_env, t in
   let transl = List.map lookup_transform drv.drv_transform in
   let apply task (tr,name) =
@@ -371,13 +371,19 @@ let prepare_task drv task =
     Debug.dprintf driver_debug "Task after transformation: %s\n%a@."
       name Pretty.print_task task;
+    trace name task;
     task
   in
-  Debug.dprintf driver_debug "Task before driver's transformation\n%a@."
-    Pretty.print_task task;
-  let task = update_task drv task in
-  Debug.dprintf driver_debug "Task after update\n%a@."
-    Pretty.print_task task;
-  List.fold_left apply task transl
+  Trans.with_checkpoint_callback trace (fun () ->
+    Debug.dprintf driver_debug "Task before driver's transformation\n%a@."
+      Pretty.print_task task;
+    let task = update_task drv task in
+    Debug.dprintf driver_debug "Task after update\n%a@."
+      Pretty.print_task task;
+    trace "driver-update" task;
+    List.fold_left apply task transl)
+
+let prepare_task drv task =
+  prepare_task_trace (fun _ _ -> ()) drv task
 
 let print_task_prepared ?old drv fmt task =
   let p = match drv.drv_printer with
diff --git a/src/transform/discriminate.ml b/src/transform/discriminate.ml
index 3e304499e..e3cff473e 100644
--- a/src/transform/discriminate.ml
+++ b/src/transform/discriminate.ml
@@ -454,6 +454,7 @@ let move_typedecl_top =
 let discriminate env = Trans.seq [
   Eliminate_algebraic.compile_match;
   Libencoding.monomorphise_goal;
+  Trans.checkpoint "discriminate_if_poly:monomorphise_goal";
   select_insts env;
   Trans.print_meta Libencoding.debug meta_lsinst;
   Trans.print_meta Libencoding.debug meta_alginst;
diff --git a/src/transform/encoding.ml b/src/transform/encoding.ml
index 803016c64..538cba20a 100644
--- a/src/transform/encoding.ml
+++ b/src/transform/encoding.ml
@@ -106,12 +106,17 @@ let keep_field_types =
 
 let encoding_smt env = Trans.seq [
   Libencoding.monomorphise_goal;
+  Trans.checkpoint "encoding_smt_if_poly:monomorphise_goal";
   select_kept def_enco_select_smt env;
+  Trans.checkpoint "encoding_smt_if_poly:select_kept";
   keep_field_types;
+  Trans.checkpoint "encoding_smt_if_poly:keep_field_types";
   Trans.print_meta Libencoding.debug Libencoding.meta_kept;
   Trans.trace_goal "meta_enco_kept"
     (Trans.on_flag meta_enco_kept ft_enco_kept def_enco_kept_smt env);
-  Trans.on_flag meta_enco_poly ft_enco_poly def_enco_poly_smt env]
+  Trans.checkpoint "encoding_smt_if_poly:twin";
+  Trans.on_flag meta_enco_poly ft_enco_poly def_enco_poly_smt env;
+  Trans.checkpoint "encoding_smt_if_poly:guards"]
 
 let encoding_tptp env = Trans.seq [
   Libencoding.monomorphise_goal;
diff --git a/src/core/ident.ml b/src/core/ident.ml
index 2338b98..b639c78 100644
--- a/src/core/ident.ml
+++ b/src/core/ident.ml
@@ -176,18 +176,48 @@ type preid = {
   pre_loc   : Loc.position option;
 }

+module Hpreid = Hashtbl.Make(struct
+  type t = preid
+  let equal = (==)
+  let hash = Hashtbl.hash
+end)
+
+let clone_observer = ref None
+let pending_clone_origins = Hpreid.create 17
+
+let with_clone_observer observer action =
+  let previous = !clone_observer in
+  clone_observer := Some observer;
+  try
+    let result = action () in
+    Hpreid.clear pending_clone_origins;
+    clone_observer := previous;
+    result
+  with exception_ ->
+    Hpreid.clear pending_clone_origins;
+    clone_observer := previous;
+    raise exception_
+
 let id_equal : ident -> ident -> bool = (==)
 let id_hash id = Weakhtbl.tag_hash id.id_tag
 let id_compare id1 id2 = Int.compare (id_hash id1) (id_hash id2)

 (* constructors *)

-let id_register = let r = ref 0 in fun id -> {
-  id_string = id.pre_name;
-  id_attrs  = id.pre_attrs;
-  id_loc    = id.pre_loc;
-  id_tag    = (incr r; Weakhtbl.create_tag !r);
-}
+let id_register = let r = ref 0 in fun preid ->
+  let id = {
+    id_string = preid.pre_name;
+    id_attrs  = preid.pre_attrs;
+    id_loc    = preid.pre_loc;
+    id_tag    = (incr r; Weakhtbl.create_tag !r);
+  } in
+  begin match Hpreid.find_opt pending_clone_origins preid with
+  | None -> ()
+  | Some original ->
+      Hpreid.remove pending_clone_origins preid;
+      Option.iter (fun observer -> observer original id) !clone_observer
+  end;
+  id

 let create_ident name attrs loc = {
   pre_name  = name;
@@ -210,7 +240,11 @@ let id_clone ?loc ?(attrs = Sattr.empty) id =
     | None -> id.id_loc
     | Some _ -> loc
   in
-  create_ident id.id_string aa loc
+  let clone = create_ident id.id_string aa loc in
+  Option.iter
+    (fun _ -> Hpreid.replace pending_clone_origins clone id)
+    !clone_observer;
+  clone

 let id_derive ?(attrs = Sattr.empty) nm id =
   let aa = Sattr.union attrs id.id_attrs in
diff --git a/src/core/ident.mli b/src/core/ident.mli
index 939f24d..1932b7f 100644
--- a/src/core/ident.mli
+++ b/src/core/ident.mli
@@ -80,6 +80,9 @@ val id_compare : ident -> ident -> int
 val id_equal : ident -> ident -> bool
 val id_hash : ident -> int

+val with_clone_observer :
+  (ident -> ident -> unit) -> (unit -> 'a) -> 'a
+
 (** a user-created type of unregistered identifiers *)
 type preid = {
   pre_name  : string;
diff --git a/src/printer/smtv2.ml b/src/printer/smtv2.ml
--- a/src/printer/smtv2.ml
+++ b/src/printer/smtv2.ml
@@ -229,13 +229,34 @@ let debug_print_term message t =
     Debug.dprintf debug "@.";
   end

+(* Process-local identifier substitution used only by the pinned reference
+   exporter. The ordinary printer path keeps the identity substitution. *)
+let identifier_rewriter = ref (fun (_ : formatter) (_ : ident) name -> name)
+
+let with_identifier_rewriter rewriter action =
+  let previous = !identifier_rewriter in
+  identifier_rewriter := rewriter;
+  try
+    let result = action () in
+    identifier_rewriter := previous;
+    result
+  with exception_ ->
+    identifier_rewriter := previous;
+    raise exception_
+
+let rewritten_identifier fmt id name =
+  (!identifier_rewriter) fmt id name
+
 let print_ident info fmt id =
-  pp_print_string fmt (id_unique info.info_printer id)
+  let name = id_unique info.info_printer id in
+  pp_print_string fmt (rewritten_identifier fmt id name)

 (** type *)

 let print_tv info fmt tv =
-  pp_print_string fmt (id_unique info.info_printer tv.tv_name)
+  let id = tv.tv_name in
+  let name = id_unique info.info_printer id in
+  pp_print_string fmt (rewritten_identifier fmt id name)

 (** print `(par ...)` around the given body to print *)
 let print_par info (* body *) =
@@ -281,7 +302,7 @@ let forget_var info v = forget_id info.info_printer v.vs_name

 let print_var info fmt {vs_name = id} =
   let n = id_unique info.info_printer id in
-  pp_print_string fmt n
+  pp_print_string fmt (rewritten_identifier fmt id n)

 let print_typed_var info fmt vs =
   fprintf fmt "(%a %a)" (print_var info) vs
diff --git a/src/printer/smtv2.mli b/src/printer/smtv2.mli
--- a/src/printer/smtv2.mli
+++ b/src/printer/smtv2.mli
@@ -9,3 +9,6 @@
 (*                                                                  *)
 (********************************************************************)

+val with_identifier_rewriter :
+  (Format.formatter -> Ident.ident -> string -> string) ->
+  (unit -> 'a) -> 'a
