(* SPDX-License-Identifier: LGPL-2.1-only WITH OCaml-LGPL-linking-exception *)

module FunctionKinds
  let function identity (x : 'a) : 'a = x
  val function arbitrary (x : 'a) : 'a
  let ghost predicate reflexive (x : 'a) = x = x
  val predicate abstract_predicate (x : 'a)
end
